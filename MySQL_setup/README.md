# MySQL CONFIGURATION FILES

This directory contains the MySQL configuration files, both used for automation.

#### setup_dev.sql
Creates a database and user if they not exists, it gives the user complete
permissions on the databse.

#### setup_tables.sql
Creates all tables needed for the website in the database.
